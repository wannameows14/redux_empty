const TOKEN_EXPIRATION_TIME_SECONDS = 3600 * 24 * 14;
const TOKEN_COOKIE_NAME = 'empty_token_cookie';
const USER_COOKIE_NAME = 'empty_name_cookie';

export default {
    TOKEN_EXPIRATION_TIME_SECONDS,
    TOKEN_COOKIE_NAME,
    USER_COOKIE_NAME,
};
